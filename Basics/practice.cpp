#include<iostream>
using namespace std;

int main (){
    // swap two numbers without using temp
   /*  int a = 4;
    int b = 6;
    a^=b,b^=a,a^=b;
    cout<<a<<" "<<b<<endl; */
    //print ASCII value oof char
    /* char ch;
    cout<<"Enter an character: ";
    cin>>ch;
    cout<<int(ch)<<endl; */
    //print area & Circumfrences of an circles
    /* double r;//double for big radius also 
    cout<<"Enter the radius: ";
    cin>>r;
    double PI = (22/7);
    cout<<"Area: "<<(PI*r*r)<<" "<<"Circ: "<<(2*PI*r)<<endl;  */
   /*  //Print leap year or not
    int y;
    cout<<"Enter an year: ";
    cin>>y;
    if(y%400==0 && y%4==0 && y%100!=0){
        cout<<"The year is leap year";
    } else{ cout<<"The year is not an leap year";} */
     

    return 0;
}